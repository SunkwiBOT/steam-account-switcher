{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the main window: core APIs, the opener used for the project page, and the application commands for everything Steam related.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
